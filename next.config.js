/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.pexels.com',
      'www.referenseo.com',
      'media.cloudbooklet.com'
    ]
  }
}

module.exports = nextConfig
